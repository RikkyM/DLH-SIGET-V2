export type JTS = {
  id: number;
  nama: string;
}

export type Departments = {
  id: number;
  nama: string;
};

export type Penugasan = {
    id: number;
    nama: string;
}

export type JenisKendaraan = {
  id: number;
  nama: string;
}